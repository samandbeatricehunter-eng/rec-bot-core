// The Tournaments feature has been removed. This page is kept as a placeholder so the
// /tournaments route (and any bookmarks/nav links to it) don't 404.
export function TournamentsPage() {
  return (
    <div className="site-page">
      <h1>Tournaments</h1>
      <p className="site-muted">Tournaments are not currently available.</p>
    </div>
  );
}
