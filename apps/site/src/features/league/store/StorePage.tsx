/** League Store body — cleared for redesign; site owns the route shell + Team rail. */
export function StorePage() {
  return <section className="hub-section hub-store" aria-label="League Store" />;
}
