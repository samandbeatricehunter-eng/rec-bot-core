/** View Rosters body — cleared for redesign; site owns the route shell + Team rail. */
export function RosterPage() {
  return (
    <div className="hub-page">
      <section className="hub-section" aria-label="View Rosters" />
    </div>
  );
}
