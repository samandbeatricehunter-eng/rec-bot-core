/** Trade Center body — cleared for redesign; site owns the route shell + Team rail. */
export function TradesPage() {
  return (
    <div className="hub-page">
      <section className="hub-section" aria-label="Trade Center" />
    </div>
  );
}
