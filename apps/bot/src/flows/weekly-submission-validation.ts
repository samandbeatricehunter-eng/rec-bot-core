export function validateStatValues(category:string,values:Record<string,number>){for(const [key,value] of Object.entries(values))if(!Number.isFinite(value)||value<0)throw new Error(`${key} must be zero or a positive number.`);if(values.completions>values.attempts)throw new Error("Completions cannot exceed attempts.");if(values.fg_made>values.fg_attempted||values.xp_made>values.xp_attempted)throw new Error("Made kicks cannot exceed attempts.");if(!category)throw new Error("A stat category is required.");return values;}
export function validPlayerName(value:string){const name=value.trim().replace(/\s+/g," ");return /^[\p{L}'-]+\s+[\p{L}' .-]+$/u.test(name)&&name.length<=80;}
