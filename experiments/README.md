# Experiments

Parked / unfinished subsystems that are **not** wired into production CI or Railway deploys.

These folders are outside `pnpm-workspace.yaml` (`apps/*`, `packages/*`) so `pnpm -r typecheck|test|build` will not run them.
