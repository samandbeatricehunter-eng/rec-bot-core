# Setup Flow

## Server Setup

/menu → Admin Panel → Server Setup → danger modal → type CONFIRM → register server.

## League Setup

/menu → Admin Panel → League Setup → danger modal → type CONFIRM → create/link league → create/load teams → user/team linking → validation report.

User/team linking is a permanent core feature and also a primary part of League Setup.
