# Historical Implementation Summary

This file is retained only as a historical note from the pre-trim launch/testing pass. It references deleted advance/import modules and old menu names.

Current platform-trim status:

- Canonical menu status: `docs/menu-map.md`
- Current runtime entry: `apps/bot/src/index-timeout.ts`
- Current API route registry: `apps/api/src/routes.ts`

Before using any older badge, advance, import, or menu guidance from prior docs, verify that the workflow is still registered in code. Several feature surfaces are now placeholders or deferred rebuild items.
